# Streamlit theme for the Rio Airbnb Pricing Lab dashboard.
[theme]
base = "light"
primaryColor = "#FF5A5F"        # Airbnb coral — intentional, not the default red
secondaryBackgroundColor = "#F7F7F7"
font = "sans serif"

[server]
headless = true
