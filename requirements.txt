# Streamlit Community Cloud installs from this file. Source of truth for versions is pyproject.toml
# ([project.dependencies] + [project.optional-dependencies].app). Kept in sync here because the
# deploy runs the app from the repo root, so `import src` resolves to ./src and CURATED_DIR points
# at the committed ./data/curated snapshot (decision A). Listed flat (no editable install) on
# purpose: installing the package would relocate PROJECT_ROOT off the committed data snapshot.
pandas>=2.2
numpy>=1.26
pyarrow>=17
statsmodels>=0.14.6
scikit-learn>=1.9.0
streamlit>=1.40
plotly>=5.24
